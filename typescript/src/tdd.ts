/**
 * Converts a currency formatted string amount into a number
 * Removes any format such as comma delimiters, currency symbol or spaces from the beginning/end
 *
 * Eg., "CRC 123,456.25" = 123456.25
 */
export function convertCurrencyStringToNumber(formattedAmount: string): number {
    // TODO
    // REGEX: /^([a-zA-Z ])*( )*|,/g
    return undefined;
}
